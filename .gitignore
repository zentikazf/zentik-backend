node_modules/
dist/
# Cache incremental de TypeScript (`incremental: true` en tsconfig). Lo reescribe CUALQUIER
# corrida de tsc o jest, asi que trackeado ensucia el git status de todos, todo el tiempo.
# Patron con comodin a proposito: hay mas de uno (tsconfig.tsbuildinfo, tsconfig.build.tsbuildinfo).
*.tsbuildinfo
.env
.env.local
.env.*.local
!.env.example
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
coverage/
*.log
npm-debug.log*
pnpm-debug.log*
.sentryclirc
uploads/

# Tooling de desarrollo local (Postgres + Redis) — no se versiona
docker-compose.yml

# Archivo local de las 39 migrations pre-baseline (2026-07-20) — no se versiona
prisma/_baseline_archive_2026-07-20/
